{
  "compilerOptions": {
    "target": "es2022",         // Set the JavaScript language version for emitted JavaScript and include compatible lib files
    "module": "commonjs",       // CommonJS modules are the standard in Node.js
    "lib": ["es2022"],          // Include type definitions for ES2022
    "outDir": "./dist",         // Redirect output structure to the directory
    "rootDir": "./src",         // Specify the root directory of input files
    "strict": true,             // Enable all strict type-checking options
    "esModuleInterop": true,    // Enables compatibility with Babel and CommonJS modules
    "moduleResolution": "node", // Resolve modules using Node.js's module resolution strategy
    "forceConsistentCasingInFileNames": true, // Disallow inconsistently-cased references to the same file.
    "allowSyntheticDefaultImports": true,     // Allow default imports from modules with no default export
  },
  "include": [
    "./src/**/*" // Include all files in the src folder,
  ],
  "exclude": [
    "node_modules" // Exclude the node_modules directory
  ]
}
